## 本版本修复

- 播放地址请求改用桌面端 User-Agent，避免移动端 UA 导致高画质接口被降级。
- 播放地址和 WBI 签名前置请求显式携带已持久化的 B 站登录 Cookie，减少 1080P 及以上清晰度只返回 720P 的情况。
- 当接口实际只返回 720P 及以下时，播放页会给出明确提示，便于区分账号/权限/取流降级和播放器问题。

## 说明

如果本版仍提示“只返回 720P 及以下”，说明播放器还没拿到高画质 URL，优先检查账号是否已在应用内登录、是否有对应视频的高画质权限，以及 Cookie 是否过期。
