## 本版本修复

- 通过 SPM 接入 FFmpegKit 包内的 `libmpv` 产品，用于直接播放 DASH 分离音视频流。
- 1080P+ 默认优先选择，不存在 1080P+ 时再按 1080P、720P 等顺序降级。
- DASH 播放不再先下载并本地合流 MP4，减少长时间卡在获取播放地址和黑屏转圈的情况。
- 1080P 及以下合流源继续保留原播放器路径，作为兼容兜底。

## 说明

这是首版 libmpv 桥接，重点验证 1080P+ 直连音视频和 CI 构建；如果真机仍有嵌入渲染或控制状态不同步问题，下一版继续接入 mpv 渲染事件和播放进度同步。
