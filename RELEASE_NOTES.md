## 本版本修复

- 根据诊断结果确认接口 `accept` 允许 1080P+，但 DASH 视频流只返回到 1080P。
- 高画质请求顺序调整为：先尝试 DURL 精确请求 1080P+/4K/HDR，再回退 DASH。
- 诊断文本新增 `durl=` 数量，方便判断高画质是否出现在 DURL 路径。

## 说明

请测试 1080P+ 菜单项。如果仍然回到 1080P，请继续反馈黄色诊断文本；重点看是否变为 `DURL selected=112/120`，以及 `durl=` 是否大于 0。
