## 本版本修复

- 播放器控制层新增取流诊断信息，显示接口实际选中的清晰度、分辨率、`accept_quality` 和 DASH 视频流 id 列表。
- 用于确认当前问题到底是播放器无法播放 1080P+，还是播放接口没有返回 1080P+ URL。
- 不改变现有播放策略，仅增加可见诊断文本，便于截图反馈。

## 说明

请在视频页点一下播放器显示控制层，把黄色诊断文本截图发回来。重点看 `accept=[...]`、`dash=[...]` 和 `selected=` 是否包含 80/112/116。
